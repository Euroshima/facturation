# Copiez ce fichier en « db_config.ini » (non versionné) à côté de l'exécutable
# ou à la racine du projet, et renseignez vos identifiants.
# Les variables d'environnement DB_HOST / DB_PORT / DB_NAME / DB_USER /
# DB_PASSWORD, si définies, ont la priorité sur ce fichier.

[database]
host = 192.168.1.10
port = 5432
name = facturation
user = VOTRE_UTILISATEUR
password = VOTRE_MOT_DE_PASSE
